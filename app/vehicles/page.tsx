import { VehicleTransferService } from "@/components/vehicle-transfer";

export const metadata = {
  title: "Vehicle Transfer & RC Services | Smart RTO",
  description: "Apply for online vehicle ownership transfer and search vehicle RC records.",
};

export default function Page() {
  return <VehicleTransferService />;
}

